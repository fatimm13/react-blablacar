# Para instalar las dependencias del proyecto escribimos en la terminal:
npm install

# Para lanzar la página web, habiendo antes lanzado el servidor flask para su funcionamiento apropiado escribimos por terminal:
npm start

Se abre [http://localhost:3000](http://localhost:3000) para verlo en nuestro navegador.